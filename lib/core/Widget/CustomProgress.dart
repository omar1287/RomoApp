import 'package:flutter/material.dart';

class CaustomProgrees extends StatelessWidget {
  const CaustomProgrees({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
